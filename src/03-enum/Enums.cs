using System;

namespace EnumSnippet
{
    public enum Actions
    {
        Start,
        Stop
    }

    public enum States
    {
        Init,
        DeInit
    }

    // init start
    // deinit start
    // init stop
    // deinit stop
}